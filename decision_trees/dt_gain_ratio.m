function [val] = dt_gain_ratio(attribute, col_idx, goal, examples) 
% DT_GAIN_RATIO Compute information gain ratio for attribute.
%
% Utility function to compute the gain ratio after splitting on attribute. Note
% that this is just the information gain divided by the intrinsic information.
%
% Inputs:
% -------
%  attribute  - Dataset attribute, cell array.
%  col_idx    - Column index in examples corresponing to attribute.
%  goal       - Decision variable, cell array.
%  examples   - Training data; the final class is given by the last column.
%
% Outputs:
% --------
%  val  - Value of the gain ratio, given the attribute and examples.

% Insert your code here.
   
end
